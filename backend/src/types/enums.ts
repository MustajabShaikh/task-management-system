export enum UserRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  USER = 'user'
}

export enum TaskStatus {
  TODO = 0,
  IN_PROGRESS = 1,
  ON_HOLD = 2,
  COMPLETED = 3
}

export enum TaskPriority {
  LOW = 0,
  MEDIUM = 1,
  HIGH = 2
}